{
  "name": "ai-food-decision-assistant",
  "version": "1.0.0",
  "description": "MVP food decision assistant with recommendation and trending features",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test --test-concurrency=1 --test-isolation=none"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
